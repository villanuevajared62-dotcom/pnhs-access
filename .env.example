# Database - Required for Vercel deployment (PostgreSQL)
# Get this from Neon, Vercel Postgres, or any PostgreSQL provider
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Session Secret - Required for production
# Generate with: openssl rand -base64 32
SESSION_SECRET="your-super-secret-session-key-here-change-in-production"

# Optional: Vercel Blob (for serverless file storage)
# Generate token: vercel blob tokens create
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_token"

# Optional: Upload size limit (MB)
# If you change this, also set NEXT_PUBLIC_UPLOAD_MAX_MB to keep UI hints in sync.
UPLOAD_MAX_MB="50"
NEXT_PUBLIC_UPLOAD_MAX_MB="50"
